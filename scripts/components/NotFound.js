import React from 'react';

var NotFound = React.createClass({
  render : function() {
    return(
      <div>
        <h1>404! Page not found.</h1>
        <p>Take a picture this doesn't happen often.</p>
      </div>
    );
  }
});

export default NotFound;
